import 'package:flutter/material.dart';

PreferredSize generalTransparentAppBar = PreferredSize(
  preferredSize: const Size.fromHeight(0),
  child: AppBar(
    backgroundColor: Colors.transparent,
    elevation: 0,
  ),
);
